{
  "version": 2,
  "name": "pokedex-api",
  "builds": [
    {
      "src": "**/*.js",
      "use": "@now/node"
    }
  ],
  "routes": [
    { "src": "/tweets/(?<query>[^/]+)$", "dest": "/tweets?query=$query" }
  ]
}
